services:
  lossyloop:
    container_name: lossyloop-go
    tty: true
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /root
    volumes:
      - ./out:/root/src/out
    networks:
      - default
    environment:
      - TZ=Asia/Tokyo